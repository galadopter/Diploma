language: generic

services:
  - docker

script:
  - make docker

deploy:
  provider: releases
  # https://github.com/settings/tokens
  # Add variable TOKEN in TravisCI settings
  api_key: $TOKEN
  file:
    - "kursach.pdf"
  name: Build $(date +'%d.%m.%Y %R')
  skip-cleanup: true
